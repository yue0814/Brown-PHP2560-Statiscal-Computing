According to the requirement, we save our complete code into .R file including clean_data.R, make_graph.R, make_table.R and make_analysis.R

The R markdown file, saved data, graphs and tables are in the results folder.

The R markdown file only contains the code necessary for generating the report.